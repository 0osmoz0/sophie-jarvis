# Dependencies
node_modules/
dist/
*.tsbuildinfo

# Native addon build output (rebuild with npm run build:native)
build/
*.node

# OS / IDE
.DS_Store
.idea/
.vscode/

# Env / secrets (none expected in Phase 1)
.env
.env.*

# Logs
*.log

# Phase 3 test sandbox (recreated by smoke tests)
tools/.tmp/

# Phase 16 local memory persistence (never commit user memories)
.jarvis/
